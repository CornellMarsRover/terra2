# Copy this file to `.env` (which is gitignored) and fill in real values.
#   cp .env.example .env
#
# Never commit `.env` itself.

# SSH password for the Jetson rover (cmr@192.168.1.69). Used by
# scripts/jetson_launcher.py via sshpass.
JETSON_SSH_PASSWORD=
