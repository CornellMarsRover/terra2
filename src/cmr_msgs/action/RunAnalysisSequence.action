# TODO(post-urc): pause/resume deferred. See docs/post_urc_backlog.md.
# Today: run-to-completion with hard abort. Cancel returns the action with
# success=false; the rover stays in whatever state it was in. Recovery is
# the operator's problem.

# Goal
# TODO(astrotech-q-4): `sequence_id` may be a site id; rename if so.
int8 sequence_id
---
# Result
bool success
string message
int32 last_completed_step
---
# Feedback
int32 current_step
int32 total_steps
string current_step_description
float32 elapsed_seconds
