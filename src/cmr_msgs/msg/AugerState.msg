std_msgs/Header header
# TODO(astrotech-q-1): rename if non-moteus controller (e.g. Maxon encoder counts).
float32 position_rev
# TODO(astrotech-q-1): rename if non-moteus controller.
float32 velocity_rev_s
float32 torque_nm
bool is_moving
